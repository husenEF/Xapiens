import React from 'react';

import {View, Text, Button} from 'react-native';

const Login = () => {
  const [loading, get, post] = useFether();
  cosnt [loading, submit]=useForm()
  return (
    <View>
      <Text>Login Screen</Text>
    </View>
  );
};

export default Login;
